name: mongo-do
version: 0.1.0
description: MongoDB SDK for the .do platform - Natural Language First, AI-Native

authors:
  - .do Platform <platform@do.gt>

license: MIT

crystal: ">= 1.10.0"

dependencies:
  json_mapping:
    github: crystal-lang/json_mapping

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    version: ~> 1.6

targets:
  spec:
    main: spec/spec_helper.cr

scripts:
  test: crystal spec
  lint: bin/ameba
